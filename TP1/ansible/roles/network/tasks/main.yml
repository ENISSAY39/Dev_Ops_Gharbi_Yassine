---
- name: Create front network
  community.docker.docker_network:
    name: front-network
    pull : true
   


- name: Create back network
  community.docker.docker_network:
    name: back-network
